# CES Analytics

This is the R Shiny analytics platform for PIH Mexico / Compañeros en Salud.

## Deploying

`./build.sh` to build the `shinyproxy` Docker image

`docker-compose up -d` to run everything
