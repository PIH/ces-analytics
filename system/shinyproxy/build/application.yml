proxy:
  port: 8080
  authentication: keycloak
  useForwardHeaders: true  # not sure if necessary or not
  admin-groups: admins
  keycloak:
    realm: shinyproxy
    auth-server-url: http://192.168.1.106:9080/auth
    resource: shinyproxy                                                  
    credentials-secret:   # interpolated by build
  docker:
      internal-networking: true
  specs:
  - id: 01_hello
    display-name: Brandon's Hello Application
    description: It says hello
    container-image: shiny-hello
    container-network: sp-net
  - id: 06_tabsets
    container-cmd: ["R", "-e", "shinyproxy::run_06_tabsets()"]
    container-image: openanalytics/shinyproxy-demo
    container-network: sp-net
logging:
  file:
    shinyproxy.log
